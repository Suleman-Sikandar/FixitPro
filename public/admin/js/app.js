// Fixiit Pro Admin JS
document.addEventListener('DOMContentLoaded', () => {
    console.log('Admin Panel Loaded');

    // Dropdown Toggles are now handled via CSS hover for a smoother experience.
    // Click logic is removed as per requirements.
});
